@model List<BookStore.VModels.Address.AddressViewModel>
@{
    Layout = null;
    //var test = ViewBag.Test;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @foreach (var item in Model)
    {
        <div>
            <h4>@item.CountryId</h4>
            <h5>@item.AddressId</h5>
        </div>
    }
</body>
</html>
