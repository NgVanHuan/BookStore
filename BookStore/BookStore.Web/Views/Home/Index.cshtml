@model IEnumerable<BookStore.VModels.Address.AddressViewModel>

@{
    ViewData["Title"] = "Home Page";
}

@*<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">First</th>
            <th scope="col">Last</th>
            <th scope="col">Handle</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.AddressId</th>
                <td colspan="2">@item.CountryId</td>
                <td>@item.StreetNumber</td>
            </tr>
        }
    </tbody>
</table>
*@